---
import ToolPage from '../../layouts/ToolPage.astro';
import Bech32Decoder from '../../components/tools/Bech32Decoder.astro';
import { TOOLS, TOOL_LABELS } from '../../data/tools-meta';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'ko' } },
    { params: { lang: 'ja' } },
  ];
}

const { lang } = Astro.params;
const tool = TOOLS.find(t => t.slug === 'bech32-decoder')!;
---

<ToolPage
  title={tool.title[lang!] || tool.title.en}
  description={tool.description[lang!] || tool.description.en}
  lang={lang!}
  canonicalPath={`/${lang}/bech32-decoder/`}
  keywords={tool.keywords}
  toolName={TOOL_LABELS['bech32-decoder'][lang!] || TOOL_LABELS['bech32-decoder'].en}
>
  <Bech32Decoder />
</ToolPage>
