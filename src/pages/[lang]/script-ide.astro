---
import ToolPage from '../../layouts/ToolPage.astro';
import ScriptIDE from '../../components/tools/ScriptIDE.astro';
import { TOOLS, TOOL_LABELS } from '../../data/tools-meta';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'ko' } },
    { params: { lang: 'ja' } },
  ];
}

const { lang } = Astro.params;
const tool = TOOLS.find(t => t.slug === 'script-ide')!;
---

<ToolPage
  title={tool.title[lang!] || tool.title.en}
  description={tool.description[lang!] || tool.description.en}
  lang={lang!}
  canonicalPath={`/${lang}/script-ide/`}
  keywords={tool.keywords}
  toolName={TOOL_LABELS['script-ide'][lang!] || TOOL_LABELS['script-ide'].en}
>
  <ScriptIDE />
</ToolPage>
