---
import ToolPage from '../../layouts/ToolPage.astro';
import LightningDecoder from '../../components/tools/LightningDecoder.astro';
import { TOOLS, TOOL_LABELS } from '../../data/tools-meta';

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'ko' } },
    { params: { lang: 'ja' } },
  ];
}

const { lang } = Astro.params;
const tool = TOOLS.find(t => t.slug === 'lightning-decoder')!;
---

<ToolPage
  title={tool.title[lang!] || tool.title.en}
  description={tool.description[lang!] || tool.description.en}
  lang={lang!}
  canonicalPath={`/${lang}/lightning-decoder/`}
  keywords={tool.keywords}
  toolName={TOOL_LABELS['lightning-decoder'][lang!] || TOOL_LABELS['lightning-decoder'].en}
>
  <LightningDecoder />
</ToolPage>
