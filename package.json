{
  "name": "tools.txid.uk-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name=tools-txid-uk --branch=main && notify-deploy.sh tools.txid.uk"
  },
  "dependencies": {
    "@astrojs/preact": "^5.0.2",
    "@astrojs/sitemap": "^3.7.1",
    "@txid/ui": "file:./packages/txid-ui",
    "astro": "^6.0.5",
    "preact": "^10.29.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
