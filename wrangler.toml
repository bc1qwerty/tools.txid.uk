# Cloudflare Pages deployment config
# This ensures deploy always targets the correct project
name = "tools-txid-uk"
pages_build_output_dir = "dist"

[pages]
project_name = "tools-txid-uk"
branch = "main"
